const express = require('express'),
    router = express.Router(),
    binanceController = require('../controllers/binanceController');

    /*
router.route('/data')
    .get(binanceController.getData);
    */

module.exports = router;